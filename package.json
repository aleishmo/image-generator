{
  "name": "image-generator",
  "version": "1.0.0",
  "description": "genetic algorithm practice",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "clean": "rm -rf ./dist",
    "dev": "tsc -p ./tsconfig.json --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
